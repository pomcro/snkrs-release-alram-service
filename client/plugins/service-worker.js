if (process.client) {
	// const cacheName = `v1`;
	// caches.keys().then(keyList => {
	// 	return Promise.all(
	// 		keyList.map(key => {
	// 			if (key !== cacheName) {
	// 				return caches.delete(key);
	// 			}
	// 		}),
	// 	);
	// });
}
